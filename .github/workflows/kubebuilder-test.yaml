name: Kubebuilder Tests

on: workflow_call
jobs:
  test:
    name: Run on Ubuntu
    runs-on: ubuntu-latest
    env:
      GOPRIVATE: github.com/konfidence-project
    steps:
      - name: Clone the code
        uses: actions/checkout@v4

      - name: Setup GitHub Repo Access
        uses: konfidence-project/.github/.github/actions/github-repo-access

      - name: Setup Go
        uses: konfidence-project/.github/.github/actions/setup-go

      - name: Running Tests
        run: |
          go mod tidy
          make test 